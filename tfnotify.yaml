---
ci: circleci
notifier:
  slack:
    token: $SLACK_TOKEN
    channel: CJ2G4E80Z
    bot: tfnotify
  github:
    token: $GITHUB_TOKEN
    repository:
      owner: "Masatoshi-Kouda"
      name: "terraform-with-circleci"
terraform:
  plan:
    template: |
      {{ .Title }}
      {{ .Message }}
      {{if .Result}}
      <pre><code> {{ .Result }}
      </pre></code>
      {{end}}
      <details><summary>Details (Click me)</summary>
      <pre><code> {{ .Body }}
      </pre></code></details>
