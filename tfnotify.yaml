---
ci: circleci
notifier:
  slack:
    token: $SLACK_TOKEN
    channel: CJ2G4E80Z
    bot: tfnotify
terraform:
  plan:
    template: |
      {{ .Message }}
      {{if .Result}}
      ```
      {{ .Result }}
      ```
      {{end}}
      ```
      {{ .Body }}
      ```
